/*
  # Empty Migration
  
  This migration was previously empty and had a version conflict.
  Since it contained no changes, we can safely create an empty migration
  with a new version number.
*/

-- No-op migration
SELECT 1;